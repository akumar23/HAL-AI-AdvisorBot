# Flask API Backend URL
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000

# Optional: For production deployment
# NEXT_PUBLIC_API_URL=https://your-production-api.com

# Admin Panel Configuration
# The admin panel is accessible at /control-panel-x7k9m2
# Change this path in production by renaming the folder in app/
# 
# Admin credentials are configured in the Flask backend:
# - ADMIN_USERNAME (default: admin)
# - ADMIN_PASSWORD (default: changeme)
# 
# IMPORTANT: Change the default credentials before deploying to production!
